package com.example.gamepadtcpudp.ui.theme

import androidx.compose.ui.graphics.Color
//Dark Theme Color
// Backgrounds
val DarkBackground = Color(0xFF121212) // nero profondo, ma non assoluto
val DarkSurface = 	Color(0xFF333333) // per card, modali, ecc.
val DarkTopBar = Color(0xFF3C3C3C)
// Testi
val DarkText = Color(0xFFE0E0E0)       // grigio chiaro, leggibile
val DarkSecondText = Color(0xFFB0B0B0)         // per testi secondari

// Accenti
val DarkAccentColor = Color(0xFFBB86FC)     // viola tenue, classico di Material Dark
val DarkErrorColor = Color(0xFFCF6679)      // rosso per errori

// Bordo o outline
val DarkOutlineColor = Color(0xFF2C2C2C)

//Light Theme Color

// Backgrounds
val LightBackground = Color(0xFFFFFFFF)       // bianco puro
val LightSurface = Color(0xFFB4B4B4)           // grigio chiarissimo per card o modali

// Testi
val LightText = Color(0xFF1C1C1C)               // quasi nero, ottimo contrasto
val LightSecondText = Color(0xFF444444)                // per testi secondari

// Accenti
val LightAccentColor = Color(0xFF3F51B5)            // blu elegante, sobrio
val LightErrorColor = Color(0xFFD32F2F)             // rosso per errori

// Bordo o outline
val LightOutlineColor = Color(0xFFDDDDDD)